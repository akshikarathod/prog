#include<stdio.h>
int main()
{
    int a,b;
    printf("Enter two numbers:\n");
    scanf("%d%d",&a,&b);
    if (a<b)
    {
        printf("the smallest number is the first number = %d\n",a);
    }
    else
    if(b<a)
    {
        printf("the smallest number is the second number=%d\n",b);
    }
}